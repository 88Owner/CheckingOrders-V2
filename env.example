# MongoDB Local Configuration
MONGODB_URI=mongodb://localhost:27017/OrderDetailing

# MongoDB Local Configuration
# MONGODB_URI=mongodb://localhost:27017/OrderDetailing

# MongoDB Remote Configuration (cho máy khác kết nối tới server)
# Thay YOUR_SERVER_IP bằng IP thực của server (VD: 192.168.1.100)
# MONGODB_URI=mongodb://YOUR_SERVER_IP:27017/OrderDetailing

# Session Secret (thay đổi thành chuỗi ngẫu nhiên)
SESSION_SECRET=your-session-secret-here

# Server Port
PORT=3001

# Environment
NODE_ENV=development

# ERPNext Configuration
# Lưu ý khi chạy trong Docker:
# - Nếu ERPNext chạy trên máy host: dùng host.docker.internal (Windows/Mac) hoặc IP máy host (Linux)
# - Nếu ERPNext chạy trong Docker: dùng tên service hoặc IP container
# - Nếu chạy trực tiếp (không Docker): dùng localhost
ERPNEXT_URL=http://host.docker.internal:8080
ERPNEXT_API_KEY=7da6579c83ee8ff
ERPNEXT_API_SECRET=406e4001191ddc0