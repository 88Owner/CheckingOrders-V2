version: '3.8'

services:
  ordercheck:
    build: .
    container_name: ordercheck-app
    ports:
      - "3000:3000"
    environment:
      MONGODB_URI: mongodb+srv://kantruong11:05092003Nt@nntruong.uku9iyi.mongodb.net/OrderDetailing?retryWrites=true&w=majority&appName=NNTruong
      SESSION_SECRET: change_this_session_secret_in_production
      NODE_ENV: production
    volumes:
      - ./uploads:/app/uploads
      - ./logs:/app/logs
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - ordercheck-network

networks:
  ordercheck-network:
    driver: bridge
